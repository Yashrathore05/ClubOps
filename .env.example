# Database
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DATABASE

# Auth
JWT_SECRET=super-secret-string-change-me

# Google OAuth – step-by-step: see docs/GOOGLE_OAUTH_SETUP.md
# Create OAuth client at https://console.cloud.google.com/apis/credentials (Web application)
# Add redirect URI: http://localhost:3001/auth/google/callback (dev) or https://YOUR_BACKEND/auth/google/callback (prod)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
BACKEND_URL=http://localhost:3001

# Email (Resend)
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxx
FROM_EMAIL=certificates@yourdomain.com

# Server
PORT=3001

# Optional: so backend can redirect wrong shared links to frontend
FRONTEND_APP_URL=http://localhost:3000
